{
  "name": "greek-mcp",
  "version": "1.0.2",
  "description": "MCP server: improve Greek copy with OpenAI or Claude (BYOK)",
  "type": "module",
  "bin": {
    "greek-mcp": "./bin/greek-mcp.mjs"
  },
  "files": [
    "dist",
    "bin",
    "install"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/karyofyllis/greek-mcp.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "tsc -p tsconfig.json",
    "start": "bun run dist/server.js",
    "dev": "bun --watch run src/server.ts"
  },
  "dependencies": {
    "@cfworker/json-schema": "^4.1.1",
    "@anthropic-ai/sdk": "^0.82.0",
    "@modelcontextprotocol/server": "^2.0.0-alpha.2",
    "openai": "^6.33.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2"
  }
}
